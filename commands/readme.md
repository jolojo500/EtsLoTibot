# Every scrip here gets only ran ONCE 
they register commands to the bot since the dev portal only allows definition not behavior. 

If you wish to automate the registration you are more than welcome to make a startup script of sort in index.ts, discord.js and node definitely allows that to happen but problem is re-registering on startup, discord doesnt like that.